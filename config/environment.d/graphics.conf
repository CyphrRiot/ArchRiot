# Graphics Environment Configuration
# Fixes rendering artifacts in GTK4 applications like Thunar

# Fix GTK4 artifacts without breaking Intel graphics
# Use the older, stable GL renderer instead of the new NGL renderer
export GSK_RENDERER=gl

# Note: This fixes Thunar flashing icons and mouse trails
# while maintaining hardware acceleration for Intel systems
