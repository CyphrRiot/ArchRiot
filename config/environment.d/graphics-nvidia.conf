# Graphics Environment Configuration - NVIDIA GPU
# Optimized for NVIDIA proprietary drivers

# Use stable GL renderer for GTK4 stability
export GSK_RENDERER=gl

# NVIDIA-specific optimizations
# Keep hardware acceleration enabled (NVIDIA drivers are stable)
export __GL_THREADED_OPTIMIZATIONS=1

# Note: NVIDIA drivers generally work well with GTK4
# Hardware acceleration remains enabled for best performance
