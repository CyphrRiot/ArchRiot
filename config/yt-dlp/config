# Select best video with 1080p resolution and best audio, fallback to best if 1080p unavailable
-f "bestvideo[height=1080][ext=mp4]+bestaudio[ext=m4a]/best"

# Ensure output is merged into mp4 format
--merge-output-format mp4

# Output file naming template (title only, no format ID)
-o "%(title)s.mp4"

# Optional: Avoid adding format ID to filename
--no-keep-fragments
--no-write-playlist-metafiles
